server:
  port: ${SERVER_PORT:8443}
spring:
  application:
    name: gateway-web
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:172.16.0.6}:${REGISTER_PORT:30000}
      config:
        server-addr: ${REGISTER_HOST:172.16.0.6}:${REGISTER_PORT:30000}
        file-extension: yml
    sentinel:
      transport:
        dashboard: ${SENTINEL_DASHBOARD_HOST:localhost}:${SENTINEL_DASHBOARD_PORT:9020}
